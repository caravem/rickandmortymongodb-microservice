apiVersion: apps/v1
kind: Deployment
metadata:
  name: rickandmorty-deployment
  namespace: develop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rickandmorty
  template:
    metadata:
      labels:
        app: rickandmorty
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: rickandmorty
          image: caravem/rickandmorty:5c5b7b0fa53ebc5501ff3d6e3a73384b2bab84bf
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: mongo-secret
 