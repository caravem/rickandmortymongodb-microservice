apiVersion: apps/v1
kind: Deployment
metadata:
  name: rickandmorty-deployment
  namespace: develop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rickandmorty
  template:
    metadata:
      labels:
        app: rickandmorty
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: rickandmorty
          image: caravem/rickandmorty:2a8a0b2456ddfc70dd723e7fdc405e4a4334c1ae
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: mongo-secret
 